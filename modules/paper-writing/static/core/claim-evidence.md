# 主张与证据纪律

可用证据包括真实输出、验证记录、模型定义、题面事实和已核验文献。它们承担不同作用：

- 结果主张引用 `solver/` 的实验、输出或验证。
- 模型定义引用 `modeling/` 的组件、假设或契约。
- 题意与数据事实引用 `analysis/`。
- 理论背景或外部方法引用原始且核验的文献。

`show`、`证明`、`显著优于` 等强结论需要直接证据；趋势或有限场景使用“表明”“在当前数据上”“在设定范围内”等有边界表达。论文中的限制、失败实验和验证不足必须与结果并列呈现。
