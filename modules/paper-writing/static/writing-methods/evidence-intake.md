# 第一轮：证据准入

读取题目要求、模型规格、实际结果、验证、复现清单、图表和官方规则。对每个缺口记录来源、影响和回退目标。

不要从 solver 报告的自然语言摘要中抄数字；优先引用结果清单、表格、图和验证记录。最终稿只能使用 `ready_for_writing: true` 的 solver 包。

对来自预测、分类、分组比较或重复测量的主张，同时读取分析单位、验证划分和信息边界。正文不得把主体级、时间外或成组验证的结论改写成无范围的总体结论；缺少这些记录时，在 `source-map.yaml` 中标为受限证据或回退求解阶段。
