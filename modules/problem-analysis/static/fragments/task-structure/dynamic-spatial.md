# 动态与空间结构检查

适用于 `mechanism` 或 `simulation` 标签。只写问题结构，不推荐算法。

- 登记初始状态、触发事件、状态转移、边界或终止条件。
- 区分固定、外生和可控量的生效时点；检查事件先后与同步条件。
- 明确量是瞬时、区间、累计、并集还是连续覆盖。
- 核对空间参考系、几何对象、尺度和交叠/遮挡关系。
- 用零时长、同时发生、边界接触或重叠情景挑战当前定义。
