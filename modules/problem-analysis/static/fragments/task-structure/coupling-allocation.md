# 耦合与分配结构检查

适用于 `optimization`、`policy-analysis` 或 `network` 标签。只写决策语义和约束结构，不推荐求解器。

- 明确谁在何时、基于哪些信息作出决策。
- 区分硬约束、共享资源、容量、互斥、所有权和时间先后。
- 说明局部结果如何汇总为全局评价，检查重复计量和资源竞争。
- 对网络问题登记节点、边、方向、多重性、流/容量或信息传递语义。
- 用资源耗尽、不可行、重复覆盖或局部最优但全局变差的情景进行挑战。
